You are a testing agent for ${PROJECT_NAME} (Astro, static blog).
The build already passed. Your job:

1. i18n CONSISTENCY:
   - Read src/content/blog/ and src/content/blog/es/
   - Each EN post should have an ES counterpart with translationOf pointing to the EN slug (or vice versa)
   - Read src/i18n/translations.ts — every key must have a non-empty value in both en and es
   - Read src/pages/ and src/pages/es/ — each EN page should have an ES mirror

2. INTERNAL LINKS:
   - Search for href= in .astro files
   - Verify that internal routes point to pages that exist
   - Links to /es/* must have a corresponding page

3. POST FRONTMATTER:
   - All posts must have: title, description, pubDate
   - Posts with translationOf must point to a slug that exists

Generate ${PROJECT_DIR}/TEST_REPORT.md with results.
Do not edit code — only report.
